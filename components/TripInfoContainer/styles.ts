import { StyleSheet } from "react-native";

export const TripInfoContainerStyles = StyleSheet.create({
  container: {
    width: "100%",
    gap: 4,
  },

  title: {
    fontFamily: "Roboto",
    fontSize: 16,
    color: "#666666",
    fontWeight: "bold",
  },

  description: {
    fontFamily: "Roboto",
    fontSize: 16,
    color: "#000",
    fontWeight: 700,
  },
});
